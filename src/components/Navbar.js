import React from 'react';

const Navbar = () => (
  <nav className="navbar navbar-dark bg-dark p-3">
    <span className="navbar-brand mb-0 h1">Fitness Tracker</span>
  </nav>
);

export default Navbar;
